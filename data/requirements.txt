# FastAPI and standard production dependencies
fastapi[standard]>=0.115.0

# Data processing
pandas>=2.2.0
numpy>=2.0.0

# HTTP client for external requests
httpx>=0.27.0

# Data validation (automatically included by FastAPI, but good to pin)
pydantic>=2.7.0
